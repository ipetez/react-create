import {COMPONENT_NAMEActionTypes} from '../actions/COMPONENT_NAME.actions'

const {ACTION_TYPE} = COMPONENT_NAMEActionTypes;

const initialState = {
}

const COMPONENT_NAME = (state = initialState, action) => {
	switch(action.type){
		case ACTION_TYPE:
			
			break;
	}
	return state;
}

export default COMPONENT_NAME;